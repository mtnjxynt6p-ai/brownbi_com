# GoDaddy-Optimized Requirements
# Lightweight, production-ready dependencies

# Core LangChain
langchain-core>=0.1.0
langchain-community>=0.0.13

# Vector Database (pre-built, no model loading needed)
chromadb>=0.4.0

# Embeddings (lightweight model only - don't load on server)
# sentence-transformers pre-computed locally, not needed on server
numpy>=1.24.0,<2.0.0

# AWS Bedrock for LLM
boto3>=1.34.0
botocore>=1.34.0

# Guardrails (lightweight version)
llm-guard>=0.3.0

# Observability
langfuse>=2.17.0

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0

# Optional: For guardrails that need it
requests>=2.28.0
